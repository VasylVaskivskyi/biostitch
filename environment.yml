name: biostitch
channels:
    - conda-forge
dependencies:
    - python=3.7
    - pip
    - numpy
    - pandas
    - scipy
    - dask
    - pip:
        - imagecodecs-lite
        - tifffile
        - opencv-contrib-python
